{
  "name": "securedelivery-workspace-tooling",
  "version": "1.0.0",
  "private": true,
  "description": "Simple validation tooling for SecureDelivery contracts and documentation.",
  "scripts": {
    "ci": "bash scripts/ci.sh",
    "check:docs": "node scripts/check-doc-links.mjs"
  },
  "devDependencies": {
    "@asyncapi/parser": "3.6.3",
    "@redocly/cli": "2.49.1"
  },
  "engines": {
    "node": ">=24 <25"
  }
}
